import initialState from './initialState';

export default function cameraReducer(state = initialState.cameraList, action) {

switch(action.type){
    case "FETCH_DRONE_CAMERAS":
    default:
            return state;

}

}